# Inventory Service Configuration
# This file contains settings that control the behavior of the Inventory Service.

# The inventory section contains all inventory-related settings
inventory:
  # Retry policy settings
  retry:
    # How many times to retry a failed request
    attempts: 10
    # How long to wait between retries (in seconds)
    delay: 0.2